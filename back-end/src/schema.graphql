### This file was autogenerated by GraphQL Nexus
### Do not make changes to this file directly


type AuthPayload {
  token: String!
  user: User!
}

"""DateTime"""
scalar DateTime

type HelpRequest {
  fulfiller: User
  id: ID!
  matched: Boolean!
  messages(after: String, before: String, first: Int, last: Int, skip: Int): [Message!]
  owner: User
}

type Message {
  body: String!
  createdAt: DateTime!
  id: ID!
  owner: User!
  request: HelpRequest!
  updatedAt: DateTime!
}

type Mutation {
  deleteHelpRequest(id: ID): Boolean!
  login(email: String, password: String): AuthPayload!
  sendMessage(body: String, helpRequestId: ID): Message!
  signup(email: String, name: String, password: String): AuthPayload!
  submitHelpRequest(healthScore: Int!): HelpRequest
}

type Query {
  currentConversation: HelpRequest
  helpRequests(matched: Boolean): [HelpRequest!]!
  me: User!
}

type User {
  email: String!
  fulfilledRequests(after: String, before: String, first: Int, last: Int, skip: Int): [HelpRequest!]
  helpRequests(after: String, before: String, first: Int, last: Int, skip: Int): [HelpRequest!]
  id: ID!
  name: String
}

# Migration `watch-20191005205950`

This migration has been generated by Adrian Aleixandre at 10/5/2019, 8:59:50 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "lift"."HelpRequest" ADD COLUMN "user" TEXT    REFERENCES "User"(id) ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191005204730..watch-20191005205950
--- datamodel.dml
+++ datamodel.dml
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191005205950)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191005205950'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```

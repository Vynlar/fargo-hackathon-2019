# Migration `watch-20191005145153`

This migration has been generated by Adrian Aleixandre at 10/5/2019, 2:51:53 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191005144355..watch-20191005145153
--- datamodel.dml
+++ datamodel.dml
@@ -23,6 +23,6 @@
 model HelpRequest {
   id        String @default(cuid()) @id
   owner     User   @relation("helpRequests")
   health    Int
-  fulfiller User   @relation("fulfilled")
+  fulfiller User?  @relation("fulfilled")
 }
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191005145153)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191005145153'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
